//this tutorial for clang-9 new

cmd_gm.cpp
//Find in: ACMD(do_set_stat)
	
	if (*szName == NULL || *szChangeAmount == '\0')
	
///Change
	if (!*szName || *szChangeAmount == '\0')
	
char_skill.cpp
	pkVictim->GetVID() : NULL
	
///change
	pkVictim->GetVID() : 0
	
config.cpp
//Find
	if (NULL != line[0])
	
///Change all
	if (line[0])

questlua_pc.cpp and char_item.cpp
//Find
	std::vector <LPITEM> item_gets(NULL);
	
///Change all
	std::vector <LPITEM> item_gets;